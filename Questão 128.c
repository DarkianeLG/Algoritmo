#include<stdio.h>
#include<conio.h>
#include<string.h>
void main (){
	char nome [10];
	int peso, k;
	printf("Informe o nome do boi: ");
	gets(nome);
	fflush(stdin);
	printf("Agora, o peso do boi: ");
	scanf("%d", peso);
	int maisPesado= peso;
	int maisLeve= peso;
	fflush(stdin);
	char nomePesado= nome;
	char nomeLeve= nome;
		
 getch ();
}
